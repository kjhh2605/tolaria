# Study-space reservation security fixtures

These fixtures are synthetic and intentionally fake. `study-space-secret-input.json` is the only allowlisted file that may contain the unsafe fake values used by the secret scanner tests. Sanitized fixtures, snapshots, logs, notes, and adapter responses must use redacted placeholders instead of these values.
